/* Nav.css - Menu de navegação responsivo e fixo */

@import url('https://fonts.googleapis.com/css2?family=Pacifico&display=swap');


.navbar { /* Container do menu de navegação */
  position: fixed; /* Fixa o menu no topo da tela */
  top: 0; /* Alinha no topo */
  left: 0; /* Alinha à esquerda */
  width: 100%; /* Ocupa toda a largura */
  background: var(--bg-nav); /* Cor de fundo usando variável do tema */
  color: var(--text-primary); /* Cor do texto usando variável do tema */
  display: flex; /* Layout flexível */
  align-items: center; /* Centraliza verticalmente */
  justify-content: space-between; /* Espaço entre logo e links */
  padding: 0.5rem 2rem; /* Espaçamento interno */
  z-index: 1000; /* Sobrepõe outros elementos */
  box-shadow: var(--shadow-light); /* Sombra usando variável do tema */
  min-height: 60px; /* Altura mínima */
  backdrop-filter: blur(10px); /* Efeito de desfoque */
  border-bottom: 1px solid var(--border-secondary); /* Borda inferior */
  transition: all 0.3s ease; /* Transição suave */
}

.logo { /* Estilo do nome ou logo */
  font-size: 2rem; /* Tamanho da fonte */
  font-family: 'Pacifico', cursive; /* Fonte estilizada */
  color: var(--accent-primary); /* Cor usando variável do tema */
  font-weight: 400; /* Peso da fonte */
  letter-spacing: 2px; /* Espaçamento entre letras */
  text-shadow: 1px 1px 6px rgba(0,0,0,0.12); /* Sombra do texto */
  transition: color 0.3s ease; /* Transição suave */
}

.nav-controls { /* Container dos controles (tema + menu) */
  display: flex;
  align-items: center;
  gap: 1rem;
}

/* Botão de alternar tema simples */
.theme-toggle {
  background: none;
  border: none;
  font-size: 2rem;
  cursor: pointer;
  color: var(--accent-primary, #f5c518);
  transition: color 0.3s;
  outline: none;
  margin-right: 0.5rem;
}

/* Ícones específicos para cada tema */
.theme-toggle .fa-sun {
  color: #ffd700; /* Dourado para o sol */
}

.theme-toggle .fa-moon {
  color: #c0c0c0; /* Prateado para a lua */
}

.theme-toggle:hover {
  background-color: var(--accent-primary);
  transform: scale(1.05);
  box-shadow: var(--shadow-accent);
}

.theme-toggle:hover .fa-sun {
  color: #ffffff; /* Branco no hover */
}

.theme-toggle:hover .fa-moon {
  color: #ffffff; /* Branco no hover */
}


.menu-toggle { /* Botão do menu hambúrguer */
  display: none; /* Esconde em telas grandes */
  background: none; /* Sem fundo */
  border: none; /* Sem borda */
  color: #fff; /* Cor do ícone */
  font-size: 2rem; /* Tamanho do ícone */
  cursor: pointer; /* Cursor de mãozinha */
  z-index: 1100; /* Sobrepõe o menu */
}


.nav-links { /* Lista de links do menu */
  list-style: none; /* Remove marcadores */
  display: flex; /* Layout flexível */
  gap: 2rem; /* Espaço entre links */
  margin: 0; /* Remove margens */
  transition: right 0.3s cubic-bezier(.77,0,.18,1); /* Animação lateral */
  z-index: 1050; /* Sobrepõe conteúdo */
}

.nav-links li a, /* Estilo dos links */
.nav-links li .nav-link { /* Estilo dos links com classe */
  color: var(--text-primary); /* Cor usando variável do tema */
  text-decoration: none; /* Sem sublinhado */
  font-size: 1rem; /* Tamanho da fonte */
  transition: all 0.3s ease; /* Transição suave */
  padding: 0.5rem 3rem; /* Espaçamento interno */
  border-radius: 6px; /* Cantos arredondados */
  position: relative; /* Para efeitos */
}

.nav-links li a:hover, /* Hover e ativo */
.nav-links li a.active,
.nav-links li .nav-link:hover, /* Hover para links com classe */
.nav-links li .nav-link.active {
  color: var(--accent-primary); /* Cor usando variável do tema */
  background: var(--bg-tertiary); /* Fundo sutil */
  transform: translateY(-2px); /* Elevação sutil */
}


@media (max-width: 1200px) {
  .navbar {
    padding: 0.5rem 1rem;
  }
  .logo {
    font-size: 1.5rem;
  }
}

@media (max-width: 900px) { /* Ajustes para telas médias */
  .navbar {
    padding: 0.5rem 0.5rem;
  }
  .nav-links {
    gap: 0.7rem; /* Reduz espaço entre links */
  }
  .logo {
    font-size: 1.2rem;
  }
}

@media (max-width: 768px) { /* Ajustes para mobile */
  .nav-controls {
    order: 2;
  }
  
  .theme-toggle {
    font-size: 1.3rem;
    padding: 0.4rem;
  }
  
  .menu-toggle {
    display: block; /* Mostra o botão */
    margin-right: 10px; /* Espaço à direita */
    margin-left: auto; /* Empurra para a direita */
    padding: 0.5rem 0.75rem; /* Espaçamento interno */
  }
  .nav-links {
    position: fixed; /* Fixa o menu lateral */
    top: 0; /* Alinha no topo */
    right: -320px; /* Esconde fora da tela */
    height: 100vh; /* Altura total da tela */
    width: 200px; /* Largura do menu */
    background: var(--bg-nav); /* Fundo usando variável do tema */
    flex-direction: column; /* Coluna */
    gap: 2rem; /* Espaço entre links */
    padding: 4rem 1rem 1rem 1rem; /* Espaçamento interno */
    box-shadow: var(--shadow-medium); /* Sombra usando variável do tema */
    border-left: 1px solid var(--border-secondary); /* Borda lateral */
    transition: right 0.3s cubic-bezier(.77,0,.18,1); /* Animação */
    display: flex; /* Flex layout */
    align-items: flex-start; /* Alinha à esquerda */
    z-index: 1050; /* Sobrepõe conteúdo */
  }
  .nav-links.open {
    right: 0; /* Mostra o menu */
  }
  .nav-links li {
    width: 100%; /* Links ocupam toda a largura */
  }
  .nav-links li a {
    display: block; /* Ocupa toda a linha */
    width: 100%; /* Largura total */
    padding: 0.75rem 0.5rem; /* Espaçamento interno */
    font-size: 1.1rem; /* Fonte maior */
    border-radius: 4px; /* Cantos arredondados */
  }
  .nav-links li a:hover,
  .nav-links li a.active {
    background: var(--bg-tertiary); /* Fundo usando variável do tema */
    color: var(--accent-primary); /* Cor usando variável do tema */
  }
  body {
    padding-top: 60px; /* Espaço para o menu fixo */
  }
}

@media (max-width: 600px) {
  html, body {
    overflow-x: hidden;
    width: 100vw;
  }
  .navbar {
    min-height: 48px;
    padding: 0.3rem 0.2rem;
    width: 100vw;
    box-sizing: border-box;
  }
  .nav-links {
    position: fixed; /* Mantém o posicionamento fixo */
    top: 0; /* Alinha no topo */
    right: -320px; /* Esconde fora da tela - CORRIGIDO */
    height: 100vh; /* Altura total da tela */
    width: 200px; /* Largura do menu */
    background: #222; /* Fundo escuro */
    flex-direction: column; /* Coluna */
    gap: 0.3rem; /* Espaço entre links */
    padding: 3rem 0.5rem 0.5rem 0.5rem; /* Espaçamento interno */
    box-shadow: -2px 0 8px rgba(0,0,0,0.15); /* Sombra lateral */
    transition: right 0.3s cubic-bezier(.77,0,.18,1); /* Animação */
    display: flex; /* Flex layout */
    align-items: flex-start; /* Alinha à esquerda */
    z-index: 1050; /* Sobrepõe conteúdo */
    box-sizing: border-box;
  }
  .nav-links.open {
    right: 0; /* Mostra o menu quando aberto */
  }
  .nav-links li {
    width: 100%; /* Links ocupam toda a largura */
  }
  .nav-links li a {
    display: block; /* Ocupa toda a linha */
    width: 100%; /* Largura total */
    font-size: 0.95rem;
    padding: 0.5rem 0.2rem;
    box-sizing: border-box;
    border-radius: 4px; /* Cantos arredondados */
  }
  .nav-links li a:hover,
  .nav-links li a.active {
    background: #333; /* Fundo ao passar o mouse */
    color: #61dafb; /* Azul claro */
  }
}

@media (max-width: 400px) {
  html, body {
    overflow-x: hidden;
    width: 100vw;
  }
  .logo {
    font-size: 0.8rem;
    letter-spacing: 0.5px;
  }
  .nav-links {
    position: fixed; /* Mantém o posicionamento fixo */
    top: 0; /* Alinha no topo */
    right: -320px; /* Esconde fora da tela - CORRIGIDO */
    height: 100vh; /* Altura total da tela */
    width: 200px; /* Largura do menu */
    background: #222; /* Fundo escuro */
    flex-direction: column; /* Coluna */
    gap: 0.3rem; /* Espaço entre links */
    padding: 2rem 0.2rem 0.2rem 0.2rem; /* Espaçamento interno */
    box-shadow: -2px 0 8px rgba(0,0,0,0.15); /* Sombra lateral */
    transition: right 0.3s cubic-bezier(.77,0,.18,1); /* Animação */
    display: flex; /* Flex layout */
    align-items: flex-start; /* Alinha à esquerda */
    z-index: 1050; /* Sobrepõe conteúdo */
    box-sizing: border-box;
  }
  .nav-links.open {
    right: 0; /* Mostra o menu quando aberto */
  }
  .nav-links li {
    width: 100%; /* Links ocupam toda a largura */
  }
  .nav-links li a {
    display: block; /* Ocupa toda a linha */
    width: 100%; /* Largura total */
    font-size: 0.8rem;
    padding: 0.3rem 0.1rem;
    box-sizing: border-box;
    border-radius: 4px; /* Cantos arredondados */
  }
  .nav-links li a:hover,
  .nav-links li a.active {
    background: #333; /* Fundo ao passar o mouse */
    color: #61dafb; /* Azul claro */
  }
}

body {
  padding-top: 60px; /* Espaço para o menu fixo */
  box-sizing: border-box; /* Inclui borda e padding no cálculo do tamanho */
}

/* Ajustes de padding-top para diferentes breakpoints */
@media (max-width: 768px) {
  body {
    padding-top: 60px !important; /* Mantém espaço para o menu fixo */
  }
}

@media (max-width: 600px) {
  body {
    padding-top: 48px !important; /* Ajusta para altura menor do menu */
  }
}

@media (max-width: 400px) {
  body {
    padding-top: 40px !important; /* Ajusta para altura ainda menor */
  }
}
